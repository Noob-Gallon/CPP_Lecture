#include "Circle2.h"

// 지적재산권 보호를 위해 필요하다.
// 판매를 할 때는, header file을 다 준다.
// source code 파일들은 다 컴파일해서 판매한다.
// 즉, 실제 코드가 구현된 부분은 기계어로 바꿔서 라이브러리를 제공한다.

void Circle2::showMe(int x, int y) {
	return;
}
void Circle2::showMe(float x, float y) {
	return;
}